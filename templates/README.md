# ExoFrame Workspace Template

This folder contains template files used when creating a deployed ExoFrame workspace.

Files here are copied into the target workspace by `scripts/scaffold.sh` or `scripts/deploy_workspace.sh`.
